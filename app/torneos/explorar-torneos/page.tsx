import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export default function ExplorarTorneos() {
  return (
    <Card className="max-w-md">
      <CardHeader>
        <CardTitle className="text-md">Explorar Torneos</CardTitle>
      </CardHeader>
    </Card>
  );
}
